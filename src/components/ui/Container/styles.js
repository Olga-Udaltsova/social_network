import styled from "styled-components";

export const Container = styled.div`
  max-width: 1200px;
  margin: 20px auto;
  &.header {
    position: sticky;
    top: 0.1px;
    background: #c1d3fe;
  }
`;
